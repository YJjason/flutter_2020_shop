import 'package:flutter/material.dart';

class JdButton extends StatelessWidget {
  const JdButton({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('23'),
    );
  }
}
